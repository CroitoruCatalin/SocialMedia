@model IEnumerable<SocialMedia.Models.PostLike>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LikeValue)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LikeValue)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PostLikeID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PostLikeID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PostLikeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
